# Ep 01 — uses only base deps. Install the base env first:
#   uv pip install -r ../../requirements-base.txt
# (listed here for a standalone run)
langgraph==1.2.11
langchain==1.3.18
langchain-openai>=1.3
python-dotenv>=1.0
rich>=13.0